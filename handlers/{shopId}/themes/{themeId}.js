'use strict';
/**
 * Operations on /{shopId}/themes/{themeId}
 */
module.exports = {
    /**
     * List all page of the corresponding shop

     * parameters: , themeId
     * produces: 
     */
    get: function (req, res) {
        res.send(501);
    },
    /**
     * List all page of the corresponding shop

     * parameters: , themeId
     * produces: 
     */
    put: function (req, res) {
        res.send(501);
    }
};