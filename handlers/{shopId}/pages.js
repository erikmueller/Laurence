'use strict';
/**
 * Operations on /{shopId}/pages
 */
module.exports = {
    /**
     * List all page of the corresponding shop

     * parameters: 
     * produces: 
     */
    get: function (req, res) {
        res.send(501);
    },
    /**
     * List all page of the corresponding shop

     * parameters: 
     * produces: 
     */
    post: function (req, res) {
        res.send(501);
    }
};